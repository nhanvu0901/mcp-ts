# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build @ai-gateway/gateway`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t ai-gateway-gateway`.
FROM docker.asseco-ce.com/ai_dev/docker/python_node:python3.13-nodejs24

ENV HOST=0.0.0.0
ENV PORT=3000

WORKDIR /app

RUN addgroup --system ai-gateway-gateway && \
    adduser --system --ingroup ai-gateway-gateway ai-gateway-gateway

COPY apps/gateway/dist ai-gateway-gateway/
COPY package*.json ai-gateway-gateway/
RUN chown -R ai-gateway-gateway:ai-gateway-gateway .

# You can remove this install step if you build with `--bundle` option.
# The bundled output will include external dependencies.
RUN npm --prefix ai-gateway-gateway --omit=dev -f install

CMD [ "node", "ai-gateway-gateway/main.js" ]
